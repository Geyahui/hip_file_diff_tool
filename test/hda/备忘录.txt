【2025-8-1】
diff_hip_edited_only_on.png和  diff_hip_edited_only_off.png 记录了原始版本diff结果
主要问题是当节点状态为delete时  其下的参数会被忽略，即使取消edited_only选项也看不到数据
另外当节点状态为create时 其下的参数又总是显示，即使勾选edited_only也不会隐藏

【2025-8-1 v2】
diff_hip_edited_only_on_v2.png  diff_hip_edited_only_off_v2.png
新修版本的 无论节点增删，所有参数都参与遍历，但是默认不显示
当节点状态为 delete/create 时 如果他没有子节点，则不显示他的参数项

【2025-8-25】
test_hda_compare2 用户测试长耗时hda, 公有232个节点，数量不算多，但是filterAcceptsRow 耗时非常长
filterAcceptsRow 触发次数多，主要是 conditionForItem 递归执行了140多万次 造成的耗时长

优化前debug
T2_MapReviewer1.hda 的 节点总量是40，参数总量是774   
filterAcceptsRow("/cus_top_node")  触发约 3180次   大约是 item_data 的 4倍 
filterAcceptsRow 总触发 7490次 大约是 item_data 的 10倍 

AOE2X_GenEffect.hda 的 节点总量是232，参数总量是4344  
filterAcceptsRow("/cus_top_node")  触发约 17800次  大约是 item_data 的 4倍 
filterAcceptsRow 总触发  44300次  大约是 item_data 的 10倍

因此 filterAcceptsRow 并没有增长很多， 但是 conditionForItem 增长的不正常
查询结果  类似于 /cus_top_node/HFS_sand 这样的子hda 包括color
conditionForItem 中 会因为以下逻辑没有被成功返回 False,  因为类型是NodeType.HDANOD
        if index.data(self.data_role).node_type == NodeType.NORMAL:
            if state_value  in [ItemState.UNCHANGED]:
                return False

修改为以下逻辑后  运行时间显著降低
        if index.data(self.data_role).node_type in [NodeType.NORMAL,NodeType.HDALOCKED]:
            if state_value  in [ItemState.UNCHANGED]:
                return False


但是对比 T2_MapReviewer1.hda   AOE2X_GenEffect.hda 即使限制节点数为40， 依然会有3450次 conditionForItem触发   
这是因为  T2_MapReviewer1.hda 的差异点是顶层参数改动，因此被标记为了 EDITED ,  AOE2X_GenEffect.hda 差异点是内部节点，因此被标记为了 UNCHANGED
最终因为 下面的逻辑， 导致 AOE2X_GenEffect.hda 的 top节点被反复执行了3450多次  耗时2分钟   解除限制后  触发7840次  耗时 20s   
 
        if state_value not in [ItemState.UNCHANGED]:
            return True

houdini_base_comparator.py 中的_handle_deleted_and_edited_nodes  做如下修改后   40 个节点耗时 降低为0.6s    232节点耗时降低为  1.8s
                if source_node_data.is_root ==True :   # path == "/cus_top_node"
                    source_node_data.state = ItemState.EDITED
                    target_node_dat.state = ItemState.EDITED

结合  self._IsFilterEnabled  = False  对RecursiveFilterProxyModel 的强制限制   总运行耗时 从140s 降低为0.8s

(以上运行速度测试结果 都在 忽略 _set_icon_from_zip  的基础上) 实际  _set_icon_from_zip 正常运行下   232节点运行总耗时 7s

通过建立 icon缓存优化 后    运行耗时降低为 1.1s左右
        pixmap =  ICON_CACHE.get(icon_name)
        if  pixmap:
            item.setIcon(QIcon(pixmap))